<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RadioFinland" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ash</item>
        <item name="colorPrimaryVariant">@color/smoke</item>
        <item name="colorOnPrimary">@color/grey</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/reddark</item>
        <item name="colorSecondaryVariant">@color/redlight</item>
        <item name="colorOnSecondary">@color/smoke</item>
        <!-- Status bar color. -->
        <item name="android:textColor">?colorOnPrimary</item>
        <item name="android:actionMenuTextColor">?attr/colorOnPrimary</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
    </style>


    <style name="Headline_style">
        <item name="android:textSize">30dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="fontFamily">sans-serif-condensed</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Freq_style">
        <item name="android:textSize">25dp</item>
        <item name="fontFamily">sans-serif</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="detail_text">
        <item name="android:textSize">15dp</item>
        <item name="fontFamily">sans-serif-thin</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="Theme.RadioFinland.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RadioFinland.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RadioFinland.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>